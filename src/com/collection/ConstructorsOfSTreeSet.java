package com.collection;


import java.util.TreeSet;

public class ConstructorsOfSTreeSet {

	public static void main(String[] args) {
		
		TreeSet<Integer> ts=new TreeSet<Integer>(); // empty tree set object where all elements will be inserted in some sorting order 
		
		 
		// TreeSet<Integer> ts1=new TreeSet<Integer>(comparator c);
		// creates tree set object where all elements will be inserted in customized  sorting order
		
		// TreeSet<Integer> ts=new TreeSet<Integer>(collection c );
		// creates tree set object with all element available in collection
		
		// TreeSet<Integer> ts=new TreeSet<Integer>( sorted set );
		
		// creates tree set object with all element available in sorted set 
		
		
		
	}
}
